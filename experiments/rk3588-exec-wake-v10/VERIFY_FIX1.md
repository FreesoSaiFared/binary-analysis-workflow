V10 verifier fix1: QEMU serial filenames are launch slots, not stable logical guest IDs. Resolve logical guest traces from the `RKMESH_V10_USER_READY id=<0..3>` marker. Kernel source, ARM64 init source, source capsule, and experiment semantics are unchanged. The first executable migration boot on run 31505814128 already produced the exact V10 aggregate result; this overlay repairs only the invalid filename assumption in post-run assertions.